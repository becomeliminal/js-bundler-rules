/// <reference types="vite/client" />
// vite lets a module import a stylesheet or an asset; the compiler has to be
// told what those are. vite ships the declarations, so this is a reference
// rather than a hand-written `declare module "*.css"`.
